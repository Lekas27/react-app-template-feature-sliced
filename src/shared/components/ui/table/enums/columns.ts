/**
 * Selectable row column.
 */
export enum SelectableRowColumn {
  SELECT = "select",
}

/**
 * Actions column.
 */
export enum ActionsColumn {
  ACTIONS = "actions",
}

/**
 * Column pin position.
 */
export enum ColumnPinPosition {
  LEFT = "left",
  RIGHT = "right",
}
